# eslint 9 conflicts with @typescript-eslint 5, pulled in by react-scripts'
# eslint-config-react-app. Without this a clean `npm ci` fails with ERESOLVE,
# which breaks CI builds (Netlify installs from scratch).
legacy-peer-deps=true
